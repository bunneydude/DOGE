#include <stdint.h>
#include <stdio.h>
#include "neighbor.h"

void main()
{

	struct neighborEntry alpha;
	uint8_t i = 0;

	printf("Alpha size = %d\n", sizeof(alpha));
	printf("Test size = %d\n", sizeof(i));
}